mutation LikePhoto($input: PhotoInput!) {
    photo(input: $input) {
        id
        country {
            code
            name
            flag
        }
        description
        likes {
            total
            likes {
                user
            }
        }
    }
}